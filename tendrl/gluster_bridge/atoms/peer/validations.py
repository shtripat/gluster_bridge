
def valid_gluster_node(peer):
    # TODO(T2D1) check etcd to find if given node has a proper gluster node
    # with required gluster bits installed on it
    return True


def valid_cluster_peer(peer):
    # TODO(T2D2) check etcd to find if given node is actually part of a cluster
    return True
